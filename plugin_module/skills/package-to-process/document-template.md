# Document Template — Master Markdown Skeleton

Referenced by `workflow.md` § Step 6 and by `dispatch-writer.md`. The `sap-writer` agent uses this skeleton verbatim and fills the `<…>` placeholders from the state passed by Step 5.

## Frontmatter (YAML)

```yaml
---
package: <PACKAGE>
module: <MODULE>
sap_version: <S4|ECC>
abap_release: <e.g., 758>
industry: <from config.json>
country: <iso, from config.json>
active_modules: [<list>]
generated_at: <ISO-8601 timestamp>
generator: sc4sap:package-to-process
entry_points: [<PROG1>, <PROG2>, ...]
process_count: <N>
language: <ko|en|ja>
---
```

## Top-Level Outline

```markdown
# 📋 <PACKAGE> — End-to-End Business Process

> Industry: <industry> · Country: <country> · Modules: <active_modules joined>

## Table of Contents
- [0. Overview](#0-overview)
- [1. Process: <label1>](#1-process-<slug1>)
- [2. Process: <label2>](#2-process-<slug2>)
- ...
- [Cross-Module Notes](#cross-module-notes)
- [External Boundary Index](#external-boundary-index)
- [Sensitive Objects](#sensitive-objects)
- [Open Questions](#open-questions)

## 0. Overview

<2–4 sentences describing what this package does at a 30,000-ft view.>

### Macro Flow

![<PACKAGE> 매크로 프로세스 흐름](_assets/process-<YYYYMMDD>-<lang>/macro.png)

<details><summary>Mermaid 원본 보기</summary>

```mermaid
flowchart LR
  <NodeA["PR"]> --> <NodeB["PO"]> --> <NodeC["GR"]> --> <NodeD["IR"]>
```

</details>

> Rendered by `render-process-images.mjs` from the macro graph (`{nodes,edges}`). The collapsible Mermaid is the portable fallback when the PNG can't be rendered (no headless browser).

### Entry Points

| TCode | Program | Short text | Typical persona |
|---|---|---|---|
| <TCode1> | <PROG1> | <text> | <buyer / receiver / accountant> |

---

## <N>. Process: <Canonical Flow Label>

> Confidence: <0.0–1.0> · Members: <count> programs / <count> FMs

### <N>.1 Overview
<3–6 sentence business-voice paragraph.>

### <N>.2 Representative Scenario (sequenceDiagram)

![프로세스 <N> 대표 시나리오](_assets/process-<YYYYMMDD>-<lang>/seq-<N>.png)

<details><summary>Mermaid 원본 보기</summary>

```mermaid
sequenceDiagram
    actor User as <persona, e.g., Buyer>
    participant <PROG_ENTRY> as <Z program>
    participant <FM_CORE> as <Z function module>
    participant DB as <DB tables>
    User->>+<PROG_ENTRY>: launches TCode <X>
    <PROG_ENTRY>->>+<FM_CORE>: CALL FUNCTION '<…>'
    <FM_CORE>->>+DB: INSERT/UPDATE <TABLE>
    DB-->>-<FM_CORE>: row count
    <FM_CORE>-->>-<PROG_ENTRY>: success
    <PROG_ENTRY>-->>-User: ALV / message
```

</details>

### <N>.3 Step Table

| # | Step | Actor | CBO Object | Tables | Trigger | Output |
|---|---|---|---|---|---|---|
| 1 | <e.g., Create PR> | <Buyer> | <PROG_X> | EBAN, EBKN | TCode ME51N | Saved PR |
| 2 | <…> | <…> | <…> | <…> | <…> | <…> |

### <N>.4 External Boundary (1-hop)

| Direction | External Object | Type | Called From | Purpose |
|---|---|---|---|---|
| OUT | BAPI_PO_CREATE1 | Std BAPI | <Z program> | Create standard PO |
| IN | <Other Z package> | Custom | <our Z FM> | Cross-package call |

> Boundary = ONE hop only. Deeper traversal is intentionally out of scope.

### <N>.5 Cross-Module Notes
> Show this subsection only when this process touches a 2nd module (per `active_modules` matrix).

- <Concrete sentence, e.g., "Calls `BAPI_ACC_DOC_POST` into FI — creates accounting document on GR posting.">

---

## Cross-Module Notes (Package-level)

<Synthesis across processes. Use the `common/active-modules.md` matrix to check expected-but-missing integration (cross-module gaps).>

| Module Pair | Where it appears | Standard touchpoint | CBO override? |
|---|---|---|---|
| MM ↔ FI | Process 3 | BAPI_ACC_DOC_POST | None |

## External Boundary Index

> Consolidated list of all external (out-of-package) calls grouped by direction.

| Object | Type | Direction | Caller / Callee in package | Used by Process # |
|---|---|---|---|---|

## Sensitive Objects

> Sourced from `inventory.json → objects[].sensitive` (PII / HR / BANK / PRICE flags). Listed for awareness; row-level access remains gated by `data-extraction-policy`.

| Object | Type | Sensitivity Reason |
|---|---|---|

## Open Questions

- [ ] <Question 1 — surfaced by the analyst during Step 5, e.g., "Is `ZMM_PRICE_OVERRIDE` table still used? No callers detected.">
- [ ] <Question 2>

---

> Generated by `/sc4sap:package-to-process` on <ISO date>.
> Re-run the skill after major package changes to refresh.
```

---

## Renderer Constraints (writer MUST honor)

1. **Frontmatter is mandatory** — keys listed above are required; any missing → fail with a clear message.
2. **TOC anchors** use kebab-case slugs of the section titles (writer generates these — do not hand-edit IDs after render).
3. **Diagrams render as PNG, Mermaid is the fallback** — one macro **process-map** PNG at Overview (`macro.png`) + one **sequence-diagram** PNG per process (`seq-<N>.png`), each followed by a collapsible `<details>` holding the Mermaid source. The PNGs are produced by `node scripts/spec/render-process-images.mjs <spec.json> <out-dir>` (v13 `renderProcessMapSVG` + `renderSequenceDiagramSVG`) from a diagram-spec JSON the writer assembles: `{ lang, macro:{nodes,edges}, processes:[{slug,title,seq:{actors,items}}] }` — `seq.items` use `{m:[from,to],t}` (sync), `{…,r:true}` (return), `{note,over}`, `{alt|opt|loop}`…`{end}`. Assets live in `_assets/process-<YYYYMMDD>-<lang>/`, referenced relatively. Graceful degrade: no headless browser → manifest slot null → keep only the Mermaid block. stateDiagram stays OUT of scope.
4. **Process count = `process_count`** in frontmatter. Mismatch → fail.
5. **Step Table column order is fixed** — `# · Step · Actor · CBO Object · Tables · Trigger · Output`. Reorder = fail.
6. **No row-level data anywhere** — if the analyst returned sample row data by mistake, replace it with `<sample omitted — see data-extraction-policy>`.
7. **Cross-Module Notes subsection** in each process is conditional: render the heading only if the process actually crosses modules.
8. **Sensitive Objects table** — render the heading even when empty (with "No sensitive objects flagged."), so reviewers see the section was considered.
9. **Re-generation banner** — if file already exists at the target path, the writer prepends a one-line `> Regenerated from <previous generated_at>` directly under `# 📋 <PACKAGE> — End-to-End Business Process`.
10. **File path** — `.sc4sap/processes/<MODULE>/<PACKAGE>/process-<YYYYMMDD>-<lang>.md`. Writer MUST create parent directories if missing.
